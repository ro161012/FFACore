# ============================================================
#  FFACore configuration
#  Arena regeneration + Kill Token currency + AFK zones
#  Docs: https://github.com/ro161012/FFACore
# ============================================================

# ------------------------------------------------------------
#  General
# ------------------------------------------------------------
general:
  # Prefix used on FFACore chat messages.
  prefix: "&8[&bFFACore&8]&r"
  # Load saved arenas on startup.
  auto-load-on-startup: true

# ------------------------------------------------------------
#  Arena storage
# ------------------------------------------------------------
# Arena metadata is stored in arenas.json and block snapshots in the
# snapshots/ folder, both under plugins/FFACore/.
storage:
  save-interval-minutes: 30

# ------------------------------------------------------------
#  Arena regeneration
# ------------------------------------------------------------
regeneration:
  # STANDARD, PHASED, SELECTIVE, WAVE or WORLD_EDIT.
  default-mode: STANDARD
  # How many arenas may regenerate at the same time.
  max-concurrent: 2
  # Max milliseconds of block placement per tick.
  tick-budget: 15
  # Blocks placed per tick in STANDARD/SELECTIVE modes.
  batch-size: 1000
  # Teleport players inside an arena to its spawn before restoring blocks.
  teleport-players-to-spawn: true
  phased:
    blocks-per-second: 50000
    delay-between-phases: 2
  selective:
    track-changes: true
    fallback-to-standard: true
  wave:
    wave-speed: 10000
    reverse-order: false
  world-edit:
    use-schematic-fallback: true
    schematic-folder: schematics

# ------------------------------------------------------------
#  Arena selection
# ------------------------------------------------------------
selection:
  # Item given by /ffa arena wand.
  default-tool: STICK
  # Largest allowed arena selection in blocks.
  max-selection-size: 1000000
  preview:
    particle-type: VILLAGER_HAPPY
    particle-count: 1
    particle-interval: 0.5

# ------------------------------------------------------------
#  Arena scheduling
# ------------------------------------------------------------
schedule:
  check-interval-seconds: 1

# ------------------------------------------------------------
#  Arena GUI
# ------------------------------------------------------------
gui:
  title: "&8Arena Management"
  rows: 6
  auto-update-seconds: 5

# ------------------------------------------------------------
#  Performance
# ------------------------------------------------------------
performance:
  use-async-save: true
  use-async-load: true
  cache-snapshots: true
  # Maximum snapshot lists kept in memory before eviction.
  max-cached-snapshots: 10
  # GZIP-compress snapshots on disk.
  compress-snapshots: true

# ------------------------------------------------------------
#  Kill Token currency
# ------------------------------------------------------------
# Seconds a killer<->victim pair must wait before another token can drop
# between the same two players (in either direction).
cooldown-seconds: 60

# Tokens dropped per qualifying kill (before the streak multiplier).
tokens-per-kill: 1

# Warn the killer when the pair cooldown suppresses a drop.
notify-on-cooldown: true
cooldown-message: "&cNo Kill Token dropped - you and this player are on cooldown."

# Message sent to the killer when a token drops. Leave empty to disable.
kill-message: "&4&l+1 &cKill Token"

killstreak:
  enabled: true
  # %player% is the killer's name; %streak% is their current streak.
  message: "&4%player% &7is on a &c&l%streak% &7killstreak!"
  sound: ENTITY_EXPERIENCE_ORB_PICKUP
  announcement-minimum: 2
  reward-start: 3
  reward-step: 3
  max-token-multiplier: 5

# The Kill Token currency item is stored here as base64 bytes and updated
# automatically by /ffa killtoken set. It is seeded on first launch.
# currency-item: ""

# ------------------------------------------------------------
#  Compressed Kill Token block
# ------------------------------------------------------------
compressed-blocks:
  # Material of the Compressed Kill Token Block (64 tokens).
  compressed-block-material: QUARTZ_BLOCK

# ------------------------------------------------------------
#  AFK zones
# ------------------------------------------------------------
# Players who stand idle inside a designated AFK zone earn AFK Shards.
afk:
  enabled: true
  # How often the reward loop runs, in seconds.
  reward-interval-seconds: 30
  # Shards awarded per reward when the player qualifies.
  shards-per-interval: 1
  # A player must be idle this long (seconds) before earning shards.
  min-idle-seconds: 60
  # Hard cap on shards earned per player per hour. -1 disables the cap.
  max-shards-per-hour: 100
  # Notify the player when they earn shards.
  notify-on-earn: true
  earn-message: "&b+1 &3AFK Shard &7(the deep rewards patience)"

# ------------------------------------------------------------
#  Screenshot custom items
# ------------------------------------------------------------
# All values are read when an ability is used, so /ffa reload applies
# balance changes immediately without rebuilding the resource pack.
custom-items:
  enabled: true
  # Active abilities always use the client's rebindable swap-hands key.
  # Pressing it cancels the normal swap; move the weapon manually in the inventory
  # when you intentionally want it in the offhand.
  # The resource pack includes all of these gradient tooltip themes.
  # UNIFIED_PURPLE keeps every ability on one polished theme by default.
  tooltip-theme: UNIFIED_PURPLE
  dash-sword:
    tooltip-theme: GLOBAL
    cooldown-seconds: 20
    distance: 8
    duration-ticks: 6
  frost-sword:
    tooltip-theme: GLOBAL
    proc-chance: 0.20
    freeze-seconds: 3
  strike-sword:
    tooltip-theme: GLOBAL
    proc-chance: 0.15
  lifestealer-sword:
    tooltip-theme: GLOBAL
    proc-chance: 0.20
    heal-amount: 4
  adrenaline-blade:
    tooltip-theme: GLOBAL
    cooldown-seconds: 70
    second-chance-seconds: 4
    absorption-hearts: 14
  flux-sword:
    tooltip-theme: GLOBAL
    cooldown-seconds: 20
    damage: 24
    range: 24
    radius: 1.35
  pigxaliur:
    tooltip-theme: GLOBAL
    proc-chance: 0.20
    cooldown-seconds: 30
    hoglin-count: 3
    hoglin-lifetime-seconds: 20
  rocket-spear:
    tooltip-theme: GLOBAL
    cooldown-seconds: 30
    launch-seconds: 1
    maximum-air-seconds: 4
    launch-velocity: 1.45
    slam-velocity: 2.2
    impact-radius: 5
    impact-damage: 12
  venom-spear:
    tooltip-theme: GLOBAL
    cooldown-seconds: 12
    distance: 8
    duration-ticks: 6
    proc-chance: 0.20
    effect-radius: 20
    poison-seconds: 5
    blind-seconds: 3
  dash-spear:
    tooltip-theme: GLOBAL
    cooldown-seconds: 20
    distance: 9
    duration-ticks: 6
  vault-spear:
    tooltip-theme: GLOBAL
    cooldown-seconds: 20
    forward-velocity: 0.55
    vertical-velocity: 1.35
  paxe:
    tooltip-theme: GLOBAL
    cooldown-seconds: 20
    duration-seconds: 20
  seismic-axe:
    tooltip-theme: GLOBAL
    cooldown-seconds: 25
    launch-seconds: 0.75
    maximum-air-seconds: 3
    launch-velocity: 1.15
    slam-velocity: 1.8
    impact-radius: 8
    impact-damage: 24
    knockback: 1.35
    vertical-knockback: 0.85
  cob-axe:
    tooltip-theme: GLOBAL
    cooldown-seconds: 30
    radius: 7
  magma-axe:
    tooltip-theme: GLOBAL
    cooldown-seconds: 20
    radius: 6
    damage: 14
    fire-seconds: 4
    visual-seconds: 2
  whirl-axe:
    tooltip-theme: GLOBAL
    cooldown-seconds: 20
    radius: 6
    damage: 8
    knockback: 1.4
    vertical-knockback: 0.65
  grapple-bow:
    tooltip-theme: GLOBAL
    dash-velocity: 1.35
  volley-bow:
    tooltip-theme: GLOBAL
    cooldown-seconds: 8
    arrow-speed: 3
    spread-degrees: 7
  earthquake-mace:
    tooltip-theme: GLOBAL
    cooldown-seconds: 30
    launch-seconds: 0.75
    maximum-air-seconds: 3
    launch-velocity: 1.15
    slam-velocity: 1.8
    impact-radius: 8
    impact-damage: 12
    knockback: 1.35
    vertical-knockback: 0.85
  cob-mace:
    tooltip-theme: GLOBAL
    cooldown-seconds: 30
    radius: 7
  wither-mace:
    tooltip-theme: GLOBAL
    proc-chance: 0.20
    cooldown-seconds: 30
    wither-seconds: 10
  dash-mace:
    tooltip-theme: GLOBAL
    cooldown-seconds: 20
    dash-velocity: 1.35
